# AI CCTV feature release

Prepared 19 September 2026 from main commit 89b3401.

Includes 20 feature-specific AI-generated illustrations, 20 editorial feature guides, homepage and features-page collection, search/category filters, metadata and sitemap entries. Illustrations are explicitly labelled; guide capabilities require compatibility assessment. Google Trends returned HTTP 429, so keyword targets are relevance-based, not verified search-volume or ranking claims.

Validation: optimized production build, TypeScript and lint passed (existing unrelated warnings); all 20 guides and images returned HTTP 200; each guide has one H1 and its canonical URL; all images have distinct hashes; all 20 guides appear in the sitemap. Desktop browser rendered 20 cards without overflow.

Release isolated from unrelated uncommitted B2B redesign work. Historical local-preview notes describe earlier revisions.
